fact: function integer (n: integer) {
            do { } while ((n == 10)  && (n == 30)); {
                    delta: integer = fact(3);
                    inc(x, delta, alpha);
                    do {printHello();}
                    while (1 + 4 == 2);
                    for () {
                        while() {
                            if () {
                                x: integer = "E";
                            }
                            else
                            {
                                foo([2,3, (3,2), (e, x), ("+", "==")]);
                            }
                        }
                    }
                }
            